# Repository instructions

- Implement only what the user explicitly requests. Do not add speculative behavior,
  lifecycle hooks, abstractions, fallbacks, cleanup, or adjacent changes.
- If an unrequested change is required for correctness, explain why and ask before
  adding it.
