/** Current values of the driving controls in the UI. */
export interface ControlState {
  /** Acceleration slider value from 0 to 100 percent. */
  accelerate: number;
  /** Service-brake slider value from 0 to 100 percent. */
  brake: number;
}
